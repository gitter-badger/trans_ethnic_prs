import pandas as pd

def metric_template(
	data: pd.DataFrame,
	ref: pd.DataFrame,
	) -> float:
	"""
	data
		predicted data calculate the metrics
	ref
		reference data
	Returns
	--------

	metric
	"""
	return None
